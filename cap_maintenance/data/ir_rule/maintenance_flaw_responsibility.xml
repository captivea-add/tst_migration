<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- MANAGE 'IR.RULE' FOR 'MAINTENANCE.FLAW.RESPONSIBILITY' MODEL -->

    <!-- 'COMPANY' RULE -->
    <data>

        <!-- 'COMPANY' RULE -->
        <record model="ir.rule" id="maintenance_flaw_responsibility_rule_01">
            <field name="name">[maintenance.flaw.responsibility] - Global - Company Rule</field>
            <field name="model_id" ref="model_maintenance_flaw_responsibility"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[("company_id", "in", company_ids)]</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
